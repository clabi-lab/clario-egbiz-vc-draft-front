"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#004785",
      contrastText: "#fff",
    },
  },
});

export default theme;
